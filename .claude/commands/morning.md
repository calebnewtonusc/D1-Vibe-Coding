---
description: Morning brief alias — runs daily-brief immediately
allowed-tools: Bash(gh:*), Bash(curl:*), Bash(date:*), Bash(cat:*), Read
---

# Morning

Alias for `/daily-brief`. Run immediately — no arguments needed.

Pull Todoist tasks for today, GitHub PRs/issues, read current context, and synthesize a focused daily plan.

See `daily-brief.md` for the full logic.
